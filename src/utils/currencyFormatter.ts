export const formatUSDollar = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD'
});

export const formatNaira = new Intl.NumberFormat('en-NG', {
  style: 'currency',
  currency: 'NGN'
});
